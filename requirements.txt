# Synthetic Data Generation - Dependencies
# Compatible: Linux y Windows (Python 3.10+)
# Instalación recomendada (en orden):
#   1. PyTorch con CUDA acorde a tu sistema:
#        Linux/Windows:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#        CPU only:       pip install torch torchvision
#   2. pip install -r requirements.txt

# --- Core ML ---
torch>=2.0.0
torchvision>=0.15.0

# --- Diffusion Models (corren 100% locales, modelos cacheados en ~/.cache/huggingface/) ---
diffusers>=0.33.0
transformers>=5.0.0
accelerate>=0.33.0
safetensors>=0.4.3
peft>=0.10.0                  # Para LoRA (SDXL Lightning)
huggingface_hub>=0.25.0

# --- Cuantización (NF4 4-bit) para FLUX en GPUs con poca VRAM ---
bitsandbytes>=0.43.0          # Linux: nativo. Windows: requiere wheels precompilados (incluido)

# --- Image Processing ---
Pillow>=10.3.0
albumentations>=1.4.10,<2.0.0
scikit-image>=0.22.0

# --- Scientific Computing ---
numpy>=1.26.0,<2.0.0
scipy>=1.13.0
scikit-learn>=1.5.0

# --- Visualization ---
matplotlib>=3.8.0
tqdm>=4.66.0
plotly>=5.18.0

# --- Jupyter ---
jupyterlab>=4.2.0
ipywidgets>=8.1.0
ipykernel>=6.29.0
